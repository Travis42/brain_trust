# Brain Trust CLI Configuration
# Copy this file to .env and fill in your actual values

# Required: Your z.ai API key
ZAI_API_KEY=your_api_key_here

# Optional: z.ai API base URL (default: https://api.z.ai/v1)
ZAI_API_BASE=https://api.z.ai/v1

# Optional: Model name to use (default: glm-4)
MODEL=glm-4

# Optional: Sampling temperature for generation (default: 0.7, range: 0.0-2.0)
# Higher values (e.g., 1.0) make output more random and creative
# Lower values (e.g., 0.2) make output more focused and deterministic
TEMPERATURE=0.7

# Optional: Nucleus sampling parameter (default: 1.0, range: 0.0-1.0)
# Controls the cumulative probability threshold for token selection
# Lower values (e.g., 0.9) restrict to more likely tokens
TOP_P=1.0
